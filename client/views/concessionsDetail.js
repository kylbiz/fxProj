Template.concessionsDetail1.events({
	'click .ul-tabs li': function (event) {
		const target = event.target;
		$(target).addClass('button-change border-change').siblings().removeClass('button-change border-change');
	}
});

Template.concessionsDetail2.events({
	'click .ul-tabs li': function (event) {
		const target = event.target;
		$(target).addClass('button-change border-change').siblings().removeClass('button-change border-change');
	}
});

Template.concessionsDetail3.events({
	'click .ul-tabs li': function (event) {
		const target = event.target;
		$(target).addClass('button-change border-change').siblings().removeClass('button-change border-change');
	}
});

Template.concessionsDetail4.events({
	'click .ul-tabs li': function (event) {
		const target = event.target;
		$(target).addClass('button-change border-change').siblings().removeClass('button-change border-change');
	}
});

Template.concessionsDetail5.events({
	'click .ul-tabs li': function (event) {
		const target = event.target;
		$(target).addClass('button-change border-change').siblings().removeClass('button-change border-change');
	}
});
